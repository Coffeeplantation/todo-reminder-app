# Todoリマインダー 仕様書（デスクトップ Excel 版）

このドキュメントは `Todoリマインダー.xlsm`（VBA版）の挙動を正として記述したものです。
**ブラウザ版（`browser/index.html`）はこの仕様書に追随させること。** どちらかの版に機能追加・変更・バグ修正を行った場合は、必ずこの仕様書と、もう一方の実装の両方を更新する。

対象コード:
- `src/TodoReminder_VBA.bas`
- `src/KanbanSheetCtrl.cls`
- `src/KanbanHelper.cls`
- `src/ThisWorkbook.cls`

---

## 1. 全体構成

ブック `Todoリマインダー.xlsm` は3シートで構成される。

| シート名 | 役割 |
|---|---|
| `todoリスト` | 設定パネル + Kanbanボード（埋め込み） |
| `タスクリスト` | タスクの実データ（一覧表） |
| `名簿` | 担当者名 → メールアドレスの対応表 |

`Workbook_Open`（`ThisWorkbook.cls`）で、ブックを開くたびに以下を自動実行する。

1. `CheckAndSendReminders`（リマインダー判定・送信）
2. `SetupKanbanSheet`（Kanbanボードの再構築）

---

## 2. データモデル

### 2.1 `todoリスト` シート（設定）

| セル | 内容 | 既定値 |
|---|---|---|
| `A1` | タイトル文言（表示のみ） | `Todoリマインダー 設定` |
| `B4` (ラベル) / `C4` (値) | 既定のリマインド日数前 | `3` |
| `B5` (ラベル) / `C5` (値) | 署名（メール末尾に挿入） | `Todoリマインダーシステム` |
| `B8` (ラベル) / `C8` (値) | 自動メール送信 ON/OFF（文字列 `"ON"`/`"OFF"`） | `ON` |

- `C8` が `"OFF"`（大文字小文字を区別せず前後空白除去して比較）以外なら自動送信ONとみなす。
- `B4:C8` は角丸四角形の枠（`shp_settings_block`）で視覚的にグループ化される。
- `B11` セル以降の領域に Kanban ボード（フル表示 or 簡易表示）が図形として埋め込まれる。設定ブロックの行（4〜8行目）は Kanban のフル表示中は非表示になり、簡易表示時のみ表示される（後述）。

### 2.2 `タスクリスト` シート（列定義、A1に見出し行）

| 列 | 見出し | 内容 |
|---|---|---|
| A | No. | 連番（1始まり。空になったらそこでデータ終端とみなす） |
| B | タスク名 | 自由入力文字列 |
| C | 担当者名 | `名簿` シートの「名前」と一致させる想定 |
| D | 期日 | 日付（Excelシリアル値 or 日付として解釈できる値） |
| E | リマインド日数前 | 数値。0または未入力なら `todoリスト!C4` の既定値を使用 |
| F | ステータス | `未完了` / `進行中` / `完了` の3値（Kanbanの列に対応） |
| G | 送信済み | 空 / `済` / `アドレス不明` / `期日超過`（下記4章参照。マクロが自動更新） |
| H | 備考 | 自由入力（マクロは参照・更新しない） |

### 2.3 `名簿` シート（列定義、A1に見出し行）

| 列 | 見出し | 内容 |
|---|---|---|
| A | 名前 | `タスクリスト!C`（担当者名）と一致させるキー |
| B | メールアドレス | リマインドメールの送信先 |
| C | 部署 | 自由入力（マクロは参照・更新しない） |

---

## 3. 初回セットアップ（`SetupButtons`）

`Alt+F8` → `SetupButtons` 実行で、`todoリスト` シートに以下を配置する。

- **チェックボタン**（青、`btn_check`）: 「リマインド送信チェック」→ `CheckAndSendReminders` を実行
- **リセットボタン**（緑、`btn_reset`）: 「送信済みフラグをリセット」→ `ResetSentFlags` を実行
- 設定ブロックの枠 `shp_settings_block`
- Kanban ボード一式（`SetupKanbanSheet` 経由、3章参照）

再実行時は同名の既存図形を削除してから再生成する（多重生成防止）。

---

## 4. リマインダー判定・送信（`CheckAndSendReminders`）

`タスクリスト` の2行目から、A列（No.）が空になる行の直前まで処理する。

処理はループ内で1行ずつ、以下の優先順位で判定する（`GoTo NextRow` で次行へ）。

1. **期日（D列）が空** → スキップ（何もしない）
2. **期日が日付として解釈できない** → スキップ
3. **ステータス（F列）が「完了」** → スキップ
4. **送信済み（G列）が「済」** → スキップ（`skipCount` をカウント）
5. 残り日数 `daysLeft = 期日 - 本日`（日単位、`DateDiff("d", today, dueDate)`）を計算
   - `daysLeft < 0`（期日超過）:
     - **自動メール送信のON/OFFに関わらず** G列を `期日超過` にし、背景色を `RGB(255,99,71)`（トマト色）にする
   - `daysLeft <= reminderDays`（リマインド日数前に到達。`reminderDays` はE列、0以下ならC4の既定値）:
     - `名簿` シートを担当者名で検索し、メールアドレスを取得
     - メールアドレスが見つかれば `SendReminderEmail` を実行し、成功したらG列を `済` にして背景色 `RGB(144,238,144)`（薄緑）
     - メールアドレスが見つからなければ G列を `アドレス不明` にして背景色 `RGB(255,165,0)`（オレンジ）
   - 上記以外（まだリマインド日数に達していない）: 何もしない（G列は空のまま）

処理後、送信件数・スキップ件数をメッセージボックスで表示する。

### 4.1 メール送信（`SendReminderEmail`）

- 自動送信が **OFF** の場合: 実際の送信は行わず、判定結果（G列の更新）のみ行う（`SendReminderEmail` は常に成功扱いを返す＝ドライラン）。
- 自動送信が **ON** の場合: Outlook（`CreateObject("Outlook.Application")`）でメールを作成し、`.Display`（下書き表示。本番運用では `.Send` に変更する想定とコード内コメントにあり）。
  - 件名: `【リマインド】{タスク名}（期日：yyyy/mm/dd）`
  - 本文:
    - `daysLeft = 0` の場合: 「本日が期日のタスクがあります。ご確認をお願いいたします。」
    - それ以外: 「期日まで残り {daysLeft} 日となりました。ご対応をお願いいたします。」
    - 続けて罫線区切りで タスク名・期日（`yyyy年mm月dd日`）・残り日数を記載
    - 末尾に「このメールは{署名}より自動送信されています。」

### 4.2 フラグリセット（`ResetSentFlags`）

確認ダイアログ（Yes/No）の後、`タスクリスト` の全行についてG列の値と背景色をクリアする（`済`/`アドレス不明`/`期日超過` を全て空白に戻す）。

---

## 5. Kanban ボード（`KanbanSheetCtrl` / `KanbanHelper`）

`todoリスト` シート内に ActiveX コントロール（ListBox / CommandButton / Label）と図形を組み合わせて埋め込まれる、シート内蔵型の Kanban ボード。**2つの表示モード**を持つ。

### 5.1 表示モード

- **簡易表示（サマリー）**: 既定表示。列見出し「タスク名 / 期日 / 達成状況」+ 1本のリストボックス（`kb_sumlist`）で全タスクを一覧表示。クリックするとフルボードに切り替わる。
- **フル Kanban ボード**: 3列（未完了・進行中・完了）のリストボックスで構成される、ドラッグ&ドロップ可能な本格的な Kanban ボード。「簡易表示に戻る」ボタンでサマリーに戻る。

切替時、設定ブロック（4〜8行目）はフルボード表示中のみ表示され、簡易表示中は非表示になる（ボードがその行の位置に来るため）。図形は `Placement = xlFreeFloating` に固定し、行の表示/非表示でズレないようにしている。

### 5.2 フルボードの構成要素

| 列インデックス | 見出し | 見出し背景色 |
|---|---|---|
| 0 | 未完了 | `RGB(68,114,196)`（青） |
| 1 | 進行中 | `RGB(220,120,0)`（オレンジ） |
| 2 | 完了 | `RGB(60,150,60)`（緑） |

各列: 見出し + リストボックス（列: タスク名/担当者/期日/リマインド日数前/行番号の5列、後2列は非表示幅0）+ 「+ タスク追加」ボタン。

その他の要素:
- 列0↔1、列1↔2 間の **←→ 矢印ボタン**（選択中カードを隣接列へ移動）
- **選択を削除** ボタン（選択中カードを削除）
- **簡易表示に戻る** ボタン
- **タイトルバー**（ドラッグでボード全体を移動）
- **リサイズハンドル**（右下端をドラッグしてボード全体を拡大縮小、0.5倍〜2倍）
- **担当者ピル**（`名簿` シートの名前ごとに1つ。ドラッグして列にドロップすると、その担当者でのクイックタスク追加ダイアログが開く）
- ドラッグ中のゴーストラベル（カード/ピルの追従表示）

これら（矢印・タイトルバー・リサイズ・移動/リサイズ機能）は **Excelのシート埋め込み特有のUI** であり、レスポンシブなブラウザ画面には直接対応する概念がない。ブラウザ版では移動・リサイズ機能は対象外とし、代わりにCSSレイアウトで見やすく表示することで代替する（8章参照）。

### 5.3 カード操作

- **クリック選択**: リストボックス内の1行を選択すると、その列の見出しが黒（`RGB(20,20,20)`）にハイライトされる（他列は色を戻す）。
- **ドラッグ&ドロップ**: カードをドラッグして別列のリストボックス領域内でマウスアップすると、その列へ移動。ドラッグ判定の閾値は開始位置から6px以上動いた場合のみ（誤操作防止）。
- **矢印ボタンでの移動**: 選択中のカードがその矢印の移動元列と一致する場合のみ移動可能（そうでなければ警告メッセージ）。
- **タスク追加**（列の「+タスク追加」ボタン）: タスク名・担当者名（自由入力）・期日（`yyyy/mm/dd`、既定値は本日+7日）・リマインド日数前（既定 `3`）を順にダイアログで入力。
- **クイック追加**（担当者ピルをドロップ）: タスク名・期日・リマインド日数前を入力（担当者はドロップしたピルの名前で自動設定）。
- **削除**: 選択中のカードを確認ダイアログ後に削除。
- カードの移動・追加・削除のたびに `SaveToExcel` で `タスクリスト` シートへ即時反映（全行を一度クリアしてから列0→1→2の順に振り直して書き込む。No.列は書き込み順の連番で振り直される）。

### 5.4 データ同期

- **読み込み** (`LoadFromExcel`): `タスクリスト` の各行を、F列（ステータス）の値に応じて列0/1/2いずれかへ振り分ける（`完了`→列2、`進行中`→列1、それ以外→列0）。
- **書き込み** (`SaveToExcel`): 列0→1→2の順に全カードをタスクリストへ書き戻す。ステータス文言は列インデックスから `未完了`/`進行中`/`完了` に変換。書き込み時にG列（送信済み）は毎回空にリセットされる（＝カード移動のたびにリマインド送信判定がやり直しになる）。
- シートは `Protect DrawingObjects:=True, Contents:=False, UserInterfaceOnly:=True` で保護し、ユーザーが図形をマウスでドラッグして分解できないようにしている（コード側からの操作・ボタンクリック・リスト選択は影響を受けない）。

---

## 6. ステータス・送信済みフラグの値まとめ

**ステータス（タスクリスト!F列）**: `未完了` / `進行中` / `完了`

**送信済み（タスクリスト!G列）**:
| 値 | 意味 | 背景色 |
|---|---|---|
| （空） | 未判定 | なし |
| `済` | リマインドメール送信済み（または自動送信OFF下での送信対象確定） | 薄緑 `RGB(144,238,144)` |
| `アドレス不明` | 担当者名が名簿に見つからない | オレンジ `RGB(255,165,0)` |
| `期日超過` | 期日を過ぎている | トマト色 `RGB(255,99,71)` |

---

## 7. ブラウザ版との対応方針

- ブラウザ版 (`browser/index.html`) は本仕様書のデータモデル（設定・タスクリスト・名簿）とロジック（リマインダー判定、Kanban操作）を可能な限り忠実に再現する。
- **Outlook送信は不可**のため、ブラウザ版は常に「自動送信OFF」相当のドライラン動作とし、判定結果（G列相当のステータス）のみ更新する。送信内容（件名・本文）はログ/プレビューとして画面表示する。
- ボードの自由な移動・リサイズなど、Excelのシート埋め込み特有のUIは対象外。
- データ永続化はブラウザの `localStorage` を使用し、Excelファイルとは連携しない（開発・UI検証用途）。
- **同期ルール**: どちらかの実装（VBA / ブラウザ）に機能追加・仕様変更を行った場合は、必ず本ファイルを更新し、その内容をもう一方の実装にも反映すること。

### 7.0 プライバシー方針（完全オフライン仕様）

- **入力データ（タスク・予定・名簿・参加者・設定・APIキー）はすべてこのブラウザ内にのみ保存**する（`localStorage`＝`todoReminder.v1`、背景写真は `IndexedDB`）。同期・アップロード・アクセス解析（sendBeacon 等）の仕組みは一切持たない。
- **「🔒 完全ローカル」（`state.settings.localOnly`、既定 `true`）** がONの間は、入力内容を外部へ送らない：予定抽出・AIチャットは**PC内の Ollama のみ**（クラウドAI Claude/Gemini へは送信しない）、背景の**ネット写真（picsum.photos）も取得しない**（グラデーションにフォールバック）、保存済みのネット写真URLも再取得しない。クラウドAI／ネット写真は、この設定をOFFにして明示的に選んだ時だけ有効になるオプトイン。
- **外部CDNは読み込まない（2026-07-15、完全オフライン化）**: 以前あった Google Fonts（`fonts.googleapis.com`/`fonts.gstatic.com`）の `<link>` を撤去し、フォントはPC標準（Inter→Hiragino Sans／Yu Gothic UI／Meiryo 等）へフォールバックする。Excel解析の SheetJS はローカル同梱（`vendor/xlsx.full.min.js`）。ページ読み込み時のネットワーク要求は**自ドメイン（index.html／config.local.js／vendor）のみ**で、外部ホストへは一切リクエストしない。
- **唯一の意図的な外部アクセス＝「🔗 参照リンク」**: ユーザーがURLを入力して「読み込む」を押したときだけ、その入力先URLへ**直接**アクセスする（第三者プロキシは経由しない）。予定データを送るのではなく指定ページを読みに行く機能で、使わなければ通信は発生しない。

### 7.1 ブラウザ版の表示モデル（現状は desktop 版と異なる）

ブラウザ版は 2026-07-07 時点で、5章の「簡易表示（サマリー表）⇔フル Kanban ボード」の2画面切替をやめ、**常時ボード表示に一本化**している。列の区分け軸は「ステータス」→「期日の月」を経て、**2026-07-11 に固定の月ボード（1〜12月）を廃止し、ユーザーが自由な数だけ作成できる名前付きボード**に変更した。タスクの所属は期日から導出せず、`task.boards`（ボードid配列、複数可）で明示的に管理する。旧形式データ（`monthPos`/`monthGroups`/`extraMonths`）は読み込み時に自動移行される（タスクが存在する月＋結合に含まれる月だけを「7月」等の同名ボードへ変換し、位置・見た目・結合状態・所属を引き継ぐ）。

- ページ上部には**アプリバー（`.appbar`、sticky配置のすりガラスヘッダー）**を常時表示する（2026-07-09追加。旧タイトルバーとは別物）。内容は、アプリ名／**タスク検索ボックス**（タスク名・担当者名の部分一致。一致しないカードを `opacity: 0.22` で薄く表示する。`/` キーでフォーカス、`Esc` でクリア）／「＋タスク追加」（先頭ボードへのタスク追加ダイアログを開く）／「▦ ボード追加」（2026-07-11追加。`prompt` で名前を入力して新しいボードを作成）／「背景」（背景を即時切替）／「リマインダー」「名簿」「設定」（各共有モーダルを開く）。JSONエクスポート/インポート/全データ初期化機能は引き続き提供していない。
- **キーボードショートカット**（入力中・ダイアログ表示中は無効）: `N`=タスク追加、`B`=背景切替、`/`=検索フォーカス、`Esc`=検索クリア→カード選択解除→メニューを閉じる（この優先順で1つずつ）。
- **トースト通知**（`#toasts`、`popover="manual"` でトップレイヤー表示するためモーダルより手前に出る）: タスク追加/削除、背景切替、リマインダーチェック実行時に右下へ約4.5秒表示する。タスク削除のトーストには**「元に戻す」アクション**が付く。
- 「タスク（月別ボード）」自体の見出し・説明文、および担当者ピル（`山田太郎`等の名前表示行）も**廃止**している。
- **列＝自由ボード**（2026-07-11変更）。ボードは `state.boards`（`{id, name}` の配列）で管理し、アプリバーの「▦ ボード追加」から好きな数だけ作成できる（名前は `prompt` 入力）。各ボードの三本線メニューに「✏ 名前を変更」「🗑 ボードを削除」がある。削除時は confirm で確認し、そのボードだけに属するタスクは残りの先頭ボードへ移す（最後の1枚は削除不可）。新規タスクは指定ボード（未指定なら先頭ボード）に入る。期日とボードは独立しており、期日を変えてもタスクは移動しない。「（今月）」表示と現在月ハイライトは廃止。
- 「設定」「リマインダーチェック」「名簿」の3パネルは**画面には常に一切表示されない**（`.modal-only` により常時非表示）。「タスク（月別ボード）」だけが画面に常時表示される唯一のパネルである。
- 「設定・リマインダー・名簿」および「+タスク追加」「パネルの色」は、**展開中パネルのヘッダー右端にある三本線メニューボタン（`.kb-col-menu-btn`）から開くドロップダウンメニュー**（`.kb-col-menu`）にまとめられている（2026-07-09より**アプリバーのボタンからも同じモーダル/ダイアログを開ける**）。メニューはカード領域の上に重なる形の絶対配置オーバーレイで、パネル自体のボックスサイズには影響しない。メニュー外をクリックすると自動的に閉じる。「設定」「リマインダー」「名簿」の3パネルは**画面には常に一切表示されない**（`.modal-only` により常時非表示）。「タスク（月別ボード）」だけが画面に常時表示される唯一のパネルである。メニューの「設定・リマインダー・名簿」をクリックすると、既存のパネル要素（DOMノード）をそのまま共有モーダルへ移動して表示するため、複製ではなく常に単一のデータ・状態を共有する（どの月のメニューから開いても同じ内容・同じ操作結果になる）。モーダルを閉じるとパネルは元の位置に戻り、再び非表示になる。
- タスクボードを載せているセクション（`.board-panel`）は**透明**（2026-07-10変更。背景・枠線・影・すりガラス効果なし）で、ネット背景画像がそのまま見える。可読性は各月パネル自体の白背景（`rgba(255,255,255,0.92)`）で確保する。
- 月パネル同士は**CSSグリッドではなく完全な自由配置（絶対位置）**。各パネルは独立しており、見出し（`.kb-col-hdr`）部分をマウスドラッグすることで画面中の好きな位置へ自由に移動できる。位置は `state.boardPos[boardKey].{x,y,z}` として `localStorage` に保存され、次回表示時も維持される。初期表示時は4列×複数行のグリッド状の既定位置が割り当てられる。パネルを最後にドラッグした時ほど手前に表示される（`z` の値が大きいほど前面）。ボード領域（`#boardView`）はパネルの移動範囲に合わせて自動的にサイズが広がるが、**縦横のスクロールバーは表示しない**（`overflow: hidden`）。パネルやカードのない空き領域をマウスドラッグすると、`scrollLeft`/`scrollTop` を直接操作してボード全体をパン移動できる（`#boardView` または `.kanban-columns` 自体をクリックした場合のみパン操作が発動し、パネル本体やカードのドラッグとは干渉しない）。
- **ボード同士のドラッグ＆ドロップ結合**（2026-07-10追加、2026-07-11に自由ボード対応）: ボードの見出しをドラッグして**別のボードの上に重ねてドロップすると、1枚の結合ボードにまとまる**（例: 「営業」を「7月」に重ねると「7月＋営業」ボード）。どのボード同士でも結合でき、3枚以上にも重ねられる。ドラッグ中は結合先候補がハイライトされ、空きスペースへのドロップは従来通りの移動のみで結合しない。結合直後のトーストの「元に戻す」で取り消せる。**解除は結合ボードの三本線メニューから、どのボードを分離するか選択できる**（2026-07-11追加）: メンバーごとの「↳ 「名前」を分離」（そのボードだけ外れ、残りは結合のまま。残り1枚になる場合は全解除）と「↳ すべて解除」が並ぶ（`detachBoardFromGroup` / `unmergeBoard`）。分離したボードは結合ボードの右隣に、見た目設定を引き継いで配置される。
  - データモデル: 結合中の月の組は `state.boardGroups`（月キー配列の配列。例: `[["2026-07","2026-08"]]`）として保存。結合ボードのキーは月キーをソートして `+` で連結したもの（例: `2026-07+2026-08`）で、位置・見た目設定は `state.boardPos[結合キー]` に保持し、**結合先（ドロップされた側）の設定を引き継ぐ**。
  - **解除時の引き継ぎ**（2026-07-10追加）: 「結合を解除」すると、結合ボードの内容＝見た目設定と状態（`color`/`shape`/`border`/`fontSize`/`shadow`/`tileSize`/`icon`/`w`/`h`/`collapsed`。`INHERIT_POS_KEYS`）を**各ボードにコピーして引き継ぎ**、結合ボードのあった位置から**横に並べて再配置**する（結合ボードで未設定の項目は各ボードの元の値のまま）。タスクの所属（`task.boards`）は解除で変化しない。
  - 表示: 結合ボードには構成ボードすべてのタスクが表示され（同一タスクは1枚）、達成率（%バッジ・進捗リング）も合算で計算。展開ヘッダーは「7月＋営業」のようにボード名を「＋」連結、折りたたみタイルのバッジは各名の頭文字をカンマ連結した**横に伸びるピル型**になる。自動見出し色は先頭ボードを基準に算出し、個別色指定（メニューの「色」）も可能。
  - 操作: 結合ボードにさらに別のボード（または別の結合ボード）を重ねると3枚以上にも結合できる。結合ボードへのカードのドロップは、タスクの所属ボードが結合内に含まれていれば位置移動のみ、含まれなければ**先頭ボードへの所属変更**として扱う。ダブルクリック追加・メニューの「+タスク追加」の追加先も先頭ボードになる。
- 新規ボードは**展開状態で作成**される（2026-07-11変更。旧月ボードは既定折りたたみだった）。折りたたみ時は横幅いっぱいの帯（バー）ではなく、**幅44pxの小さなタイルに縮小**され、**ボード名の先頭2文字を入れたバッジ**（`.kb-col-count-lg`。2文字以上はピル型に伸びる）を中央に表示する（矢印アイコンや名前全文は表示しない）。タイル自体（`.kb-col`）の外枠は、そのボードのタスク達成率（`status === "完了"` の件数 ÷ 件数、**10%刻み10段階に丸め**）を12時位置から**時計回りに**描く進捗リングを兼ねる。`col.style.padding` で3px分の隙間を空け、`conic-gradient(var(--green) 0deg <達成率×3.6deg>, var(--border) <同角度> 360deg)` を `col` の背景に敷くことで、隙間からリング状に覗かせている（内側の `.kb-col-hdr` は通常のパネル色で塗りつぶされる）。達成率100%ではタイルの周囲全体が `var(--green)` の線で囲まれる。タスクが1件も無い月は進捗リング表示なし（通常の1px枠線のみ）。タイルには達成率がツールチップ（`title`属性）としても表示される。タイルをクリックすると展開し、通常幅（既定240pxまたは保存済みの `state.boardPos[boardKey].w`）の横長ヘッダー（`▼ 月名`＋達成率バッジ（`.kb-col-pct-badge`。例：`50%`。タスクが無い月は非表示）＋右端の三本線メニューボタン。月番号バッジは展開時には表示しない）とカード領域・リサイズハンドルが表示される。折りたたみ状態は `state.boardPos[boardKey].collapsed` として保存される。見出しのドラッグ（パネル移動）とクリック（開閉切り替え）は移動距離で判定して区別している。
- 展開中の各月パネルは**右下のハンドル（`.kb-col-resize`）をドラッグして大きさを自由に変更**できる。サイズは `state.boardPos[boardKey].{w,h}` として保存され、次回表示時も維持される（未変更のパネルは既定幅240px・内容に応じた自動高さのまま）。パネル内のカード領域は縦横ともスクロール可能なので、パネルを小さくしてもカードが失われることはない。**ユーザーが明示的にリサイズした後（`state.boardPos[boardKey].h` が保存済みの場合）は、カード領域の最小高さ強制（既定の220px確保）を行わず、指定したサイズへ正しく縮小できる**（保存済みの高さが無い＝未リサイズの初期状態のみ、カードが隠れないよう220px以上を自動確保する）。見出しの高さ程度まで縮めると、自動的に折りたたみ状態（円形バッジのみ）に切り替わる（flexboxの子要素は内容サイズより縮まないため、実際の描画サイズではなくドラッグ量から判定している）。
- 各ボードの見出し背景色は、**既定ではボードidから安定して決まる10色パレット**（`BOARD_COLOR_PALETTE`。同じボードは常に同じ色）で表示される（2026-07-11変更。旧「現在月に近いほど濃い」グラデーションと設定のカラーピッカー2つは廃止）。**三本線メニュー内の「色」カラーピッカーから、ボードごとに個別の色を自由に指定できる**。指定した色は `state.boardPos[boardKey].color`（hex）に保存され、パレット色より優先して適用される（バッジ・折りたたみ時のタイルにも同じ色が反映される）。
- 三本線メニューには色のほかに、月ごとに見た目を調整できるセレクトを用意している（いずれも試験的な追加要素）。
  - **形（折りたたみ時）**（`state.boardPos[boardKey].shape`。既定 `square`＝角丸四角、`circle`＝折りたたみタイルを完全な円形にする。展開中パネルの形には影響しない）
  - **枠線**（`state.boardPos[boardKey].border`。既定 `normal`＝通常の1px実線、`double`＝4px幅の二重線 `border-style: double`）
  - **文字サイズ**（`state.boardPos[boardKey].fontSize`。`small`/`normal`/`large`。カード領域のフォントサイズを `--kb-font-scale` カスタムプロパティ経由で0.85〜1.25倍に拡縮する）
  - **影の強さ**（`state.boardPos[boardKey].shadow`。`none`/`normal`/`strong`。現在月のオレンジ枠と合成して `box-shadow` に適用）
  - **折りたたみの大きさ**（`state.boardPos[boardKey].tileSize`。`small`(32px)/`normal`(44px)/`large`(60px)。折りたたみタイルと中の丸バッジのサイズを変更する）
  - **アイコン**（`state.boardPos[boardKey].icon`。空/📌/⭐/🔥/📅。展開ヘッダーの月名の前、および折りたたみタイル右上隅の小さな絵文字として表示）
- ページ全体の背景は、**ネットから取得したランダム写真**（picsum.photos。APIキー不要、seedを毎回変えて取得）を全画面に敷き、**設定した間隔（分）で自動的に別の写真へクロスフェード切替**する（2026-07-09に日替わりグラデーション方式から変更）。実装は固定配置の2枚のレイヤー（`#bgA`/`#bgB`）を `opacity` トランジションで交互に入れ替える方式で、画像の読み込み完了（`Image.onload`）を待ってから切り替えるためちらつかない。その上に可読性確保用の薄い暗色スクリム（`#bgScrim`）を重ね、コンテンツのパネル群はすりガラス（`backdrop-filter: blur`）で背景から浮かせている。取得失敗・オフライン時は旧来の10種類のグラデーション（`FALLBACK_GRADIENTS`）からランダムに1つを適用する。設定パネルの「背景：ネット画像を使う」（`state.settings.bgOnline`、既定ON。OFFでグラデーションのみ）と「背景の自動切替間隔（分）」（`state.settings.bgIntervalMin`、既定10分）で制御でき、アプリバーの「背景」ボタンまたは `B` キーでいつでも手動切替できる。
- 各カードは既定で **タスク名 / 期日 / 達成状況（＝ステータス。バッジ表示）の3項目のみ**を表示する（担当者・リマインド日数前・送信済みは非表示）。列がステータスを表さなくなったため、達成状況はカード上のバッジでのみ判別できる。
- カードをクリックすると、そのカードだけ **担当者（編集可）/ 期日（編集可、yyyy/mm/dd）/ 所属ボード（編集可、チップ）/ ステータス（編集可、ドロップダウン）/ リマインド日数前 / 送信済み状況** を含む詳細を展開表示する（再クリックで折りたたむ）。
- **所属ボード（複数選択可）**（2026-07-11に「対応月」から変更）: 展開したカードの「📌 所属ボード」に全ボードのチップ（`.em-btn`）が並ぶ。
  - チップのクリックで所属の ON/OFF ができ、`task.boards`（ボードid配列）に保存される。**最後の1つは外せない**（タスクは必ずどこかのボードに属する。外そうとするとトーストで案内）。
  - タスクは**所属ボードすべてに表示**される。カードの実体は1つの共有データなので、どのボード上のコピーで編集・ステータス変更しても全ボードに即反映される（選択展開も全コピー連動）。
  - **結合時は足す形**：結合ボードには構成ボードのタスクを合算した一覧が出る（複数所属タスクも1枚のみ）。**分離時は共有形**：複数ボード所属のタスクが双方のボードに同じ情報で表示される。
  - カードのドラッグ移動は、ドロップ先に所属ボードのいずれかが含まれていれば位置移動のみ。含まれない場合は移動元ボードの所属を外してドロップ先の先頭ボードへ加える（`moveTaskToBoard`。**期日は変更しない**）。
  - AIのシステムプロンプトのタスク一覧にも `boards`（所属ボード名の配列）として渡される。
- 「選択を削除」ボタンおよびページ下部のフッター（アプリ説明文）は**廃止**したままだが、2026-07-09より**カードの詳細展開内に「タスクを削除」ボタン**を提供する。削除は確認ダイアログなしで即時実行され、トーストの「元に戻す」で復元できる（トースト消滅後は復元不可）。
- カード領域の**空きスペースをダブルクリック**すると、そのボードを追加先としたタスク追加ダイアログが開く（期日の既定値は7日後）。
- カードは1列内では**グリッド整列ではなく自由配置**（絶対位置）。マウスドラッグで同じ月パネル内の好きな位置に動かせ、位置は `task.pos.{x,y}` として `localStorage` に保存される。カードを最後に操作した（クリック/ドラッグした）時ほど手前に表示される（`task.z` の値が大きいほど前面）。
- **カードのサイズ変更**（2026-07-10追加、2026-07-13改善）: カード右下のハンドル（`.kb-card-resize`、18px・ホバーで強調・Pointer Eventsでタッチ/ペン対応）をドラッグして大きさを自由に変更できる（最小 90×44px）。**ハンドルのダブルクリックで既定サイズに戻す**（`task.size` を破棄）。サイズは `task.size.{w,h}` として保存され、リロード後も維持される。**枠外へのはみ出し表示はカードを開いた（詳細展開した）ときだけ**（2026-07-13変更）: 通常時の `.kb-cards` は `overflow: auto`（従来どおり枠内スクロール）で、展開中のカードを含むボードにだけ `.has-open-card` が付き `overflow: visible` になる。開いたカードは**幅300px・文字サイズ拡大（12.5px基準）**で読みやすく表示され、枠に収まらなければはみ出して表示される。開いた瞬間にそのボードを最前面化（`boardPos.z` 更新）するため、はみ出し部分が隣のボードに隠れない。閉じると元のクリップ表示に戻る。カード自体は `overflow: hidden` のため縮めると内容が切れる。展開（選択）中は詳細が収まる既定レイアウト（幅220px・自動高さ）を優先し、カスタムサイズは適用しない（ハンドルも非表示）。リサイズ直後のクリックで選択がトグルしないようガードしている。
- **カードのボードバッジ**（2026-07-11に月バッジから変更）: タスクが**複数ボードに所属している場合、または結合ボード上に表示されている場合のみ**、期日の左に所属ボード名のバッジ（`.t-month`）を表示する。色はそのボードの見出しと同じ（`boardHeaderColor`。個別色指定を反映）。単独ボード上の単独所属タスクにはバッジを出さない（冗長なため）。
- カードを別のボードへドラッグして落とすと、**所属ボードが変わる**（移動元の所属を外してドロップ先へ。期日は変更しない。2026-07-11変更）。移動後の位置は、ドロップした位置に応じて移動先パネル内でクランプされる。ステータス自体は展開時のドロップダウンで変更する。
- 「+タスク追加」ボタンでの追加はボードに対して行われ、期日は7日後が既定値になる（新規タスクのステータスは常に「未完了」）。ダイアログのタイトルに追加先ボード名が出る（例「タスク追加（営業）」）。担当者はタスク追加/編集ダイアログでの手入力・選択のみで指定する（担当者一覧の表示やドラッグによるクイック追加は行わない）。
- **名簿のスコープと担当タスク表示**（2026-07-10追加）: 名簿は開き方でスコープが変わる。**アプリバーの「名簿」ボタンからは総合名簿**（タイトル「名簿（総合）」、全担当者）、**ボードの三本線メニューの「名簿」からはそのボードのタスクに参加している担当者のみ**（タイトル「名簿（営業の参加者）」等。担当者追加ボタンは非表示、該当者ゼロなら「このボードのタスクに参加している担当者はいません」）。スコープはモーダルを閉じるとリセットされる（`contactsScopeKey` 変数、非永続）。
  - 名簿の各行には**「関与タスク」列**（スコープ内でその担当者が `assignee` のタスク件数）を表示する。**行をクリックすると担当タスク一覧が展開**され（▸/▾ トグル）、各タスクが「タスク名（期日・ステータス）」のボタンとして並ぶ。
  - **タスクボタンをクリックするとモーダルを閉じてそのタスクへジャンプ**する：所属先頭ボード（結合中なら結合ボード）を展開・最前面化し、カードを選択状態（詳細展開）にして `scrollIntoView` で表示位置へスクロールする。
- 三本線メニュー（`.kb-col-menu`）の `z-index` は 100000。選択中カードが `z-index: 1000+連番` を取るため、それより常に手前に表示する（2026-07-10修正）。
- **AIアシスタント**（2026-07-10追加、ブラウザ版のみ）: アプリバーの「🤖 AI」ボタンで右下にチャットパネル（`#aiPanel`、固定配置・高さ520px）を開閉できる。
  - **プロバイダは3種類**（2026-07-12更新）: 設定の「AI：プロバイダ」（`state.settings.aiProvider`、`"anthropic"`（既定）/`"gemini"`/`"ollama"`）で選択。**Anthropic/GeminiのAPIキーを入力すると、そのプロバイダに自動で切り替わる**。プロバイダを切り替えると会話履歴（API形式が異なる）はリセットされる。
  - Claude 実装: Anthropic 公式SDK（`@anthropic-ai/sdk`）を `https://esm.run/@anthropic-ai/sdk` から動的importし、ブラウザから直接 Claude API を呼ぶ（`dangerouslyAllowBrowser: true`）。モデルは `claude-opus-4-8`、`thinking: {type:"adaptive"}`、ストリーミング応答（`client.messages.stream` + `finalMessage()`）。
  - Gemini 実装: REST（`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent?key=...`）を `fetch` で直接呼ぶ（非ストリーミング）。ツールは同じ定義を `functionDeclarations` 形式に変換して渡し（Gemini スキーマ未対応の `minimum`/`maximum` は除去）、`functionCall` パートを実行して `functionResponse` パートで返す手動ループ。エラーはHTTPステータス＋`error.message` を表示（400/403はキー確認、429はレート制限の案内を付加）。
  - **ローカルAI（Ollama）実装**（2026-07-12追加、**完全オフライン対応**）: PC内の Ollama サーバー（`http://localhost:11434/api/chat`、非ストリーミング）を `fetch` で呼ぶ。APIキー不要。モデル名は設定の「AI：ローカルモデル名」（`state.settings.ollamaModel`、既定 `qwen2.5:3b`）。ツールは OpenAI互換の `{type:"function", function:{...}}` 形式に変換して渡し、`message.tool_calls` を実行して `{role:"tool", tool_name, content}` で返す手動ループ（最大8イテレーション）。エラーは日本語で案内：接続不可→Ollama起動を案内、404（model not found）→`ollama pull` を案内、403→`OLLAMA_ORIGINS=*` 設定を案内。
    - **前提セットアップ（初回のみ・要オンライン）**: ①Ollama をインストール（`winget install Ollama.Ollama`）②ユーザー環境変数 `OLLAMA_ORIGINS=*` を設定（`file://` ページ＝Origin `null` からのCORSを許可するため。設定後 Ollama を再起動）③`ollama pull qwen2.5:3b`（約1.9GB、日本語・tool calling対応の軽量モデル）。以後はネット接続なしでAIアシスタントの全機能（会話＋タスク操作ツール）が動く。
  - APIキー: 設定パネルの「AI：Anthropic APIキー」（`state.settings.anthropicApiKey`）/「AI：Gemini APIキー」（`state.settings.geminiApiKey`、https://aistudio.google.com/apikey で発行）に入力。localStorage にのみ保存。未設定のまま送信すると選択中プロバイダに応じたエラーメッセージで設定を案内する。各キー欄の下に**「📋 コピーしたキーを貼り付け」ボタン**（2026-07-10追加。`navigator.clipboard.readText()`。Ctrl+Vが使えない環境向けのフォールバック。読み取り失敗時は手動貼り付けを案内）がある。
  - **ローカル設定ファイルからの自動読み込み**（2026-07-10追加）: `browser/config.local.js`（**git管理外**、.gitignore 済み）が存在すると `<script src="config.local.js">` で読み込まれ、`window.LOCAL_CONFIG.geminiApiKey` / `.anthropicApiKey` を**設定が未入力の場合に限り**初期値として取り込む（Geminiキーの場合はプロバイダも自動でGeminiに切替）。手入力済みのキーを上書きすることはない。ファイルが無い場合は404で単に無視される。APIキーをリポジトリにコミットせずに全ブラウザへ配る手段。
  - コンテキスト: システムプロンプトに今日の日付・全タスク（id/名前/担当/期日/ステータス/リマインド日数/送信済み）・名簿・アプリ仕様の要約を毎ターン埋め込むため、AIは集計・期限チェック・提案などに直接答えられる。
  - ツール（tool use、手動ループ・最大8イテレーション）: `add_task`（名前・期日必須。`board` でボード名を指定可、省略時は先頭ボード）/ `update_task`（id指定でステータス・期日・担当者・名前を変更、変更時は送信済みフラグをリセット）/ `delete_task` / `create_board`（ボード名を指定して新規作成。2026-07-11追加）/ `merge_boards`（ボード名の配列を指定して結合。指定ボードが結合済みならその結合ごとまとめる）/ `unmerge_board`（ボード名1つを指定してそのボードだけ分離。`all: true` で結合をすべて解除）。ボード名は完全一致優先・部分一致フォールバックで解決する。実行すると即座に `save()`＋`renderBoard()` され、トーストと、チャット内の「🔧 ツール名 を実行」表示で知らせる。システムプロンプトにはボード一覧と結合中のボード一覧も含まれる。
  - エラー処理: SDKの型付き例外（`AuthenticationError`/`RateLimitError`/`APIError`）で分岐した日本語メッセージを表示。失敗したターンは会話履歴から丸ごと巻き戻し、tool_use/tool_result が対にならない不整合を残さない。
  - 会話履歴はメモリ内のみ（リロードで消える）。「クリア」ボタンで手動リセットも可能。
  - **パネルのドラッグ移動**（2026-07-10追加）: パネルのヘッダー（「クリア」「✕」ボタン以外の部分、`cursor: move`）をドラッグすると画面中の好きな位置へ移動できる。位置は `state.aiPanelPos.{x,y}` に保存され、リロード後・再表示時も復元される（画面外に出ないよう表示時とウィンドウリサイズ時にクランプ）。既定位置は右下（`right:16px; bottom:16px`）。
  - デスクトップ版（VBA）は対象外（Excel からのHTTPS呼び出し・キー管理は別設計が必要なため未対応）。

- **自動タスク作成（予定表をAIに取り込む）**（2026-07-12追加、2026-07-13に「予定の取り込み」から改名、ブラウザ版のみ）: アプリバーの「📥 自動タスク作成」でモーダル（`sectionImport`）を開き、①参照リンク（URL）②フォルダ/ファイル選択（`webkitdirectory` / 複数ファイル）③テキスト・画像の貼り付け、の3経路で予定を読み取ってタスク候補にする。
  - **画像（スクリーンショット/写真）からの読み取り**（2026-07-15追加、ブラウザ版のみ）: 「📋 テキスト・画像の貼り付け」欄に、予定表の画像を **Ctrl+V で貼り付け**るか「📷 画像を選択」（`accept="image/*"`）で取り込むと、サムネイル（`#impImagePreview`）を表示し「🤖 抽出」で画像から予定を読み取る。画像は `FileReader` で dataURL 化し、`{ base64（接頭辞なし）, mediaType }` として `aiExtractTasks(text, image)` へ渡す。各プロバイダのビジョン入力を使う：**Ollama** は `messages[].images:[base64]`（モデルは新設定 `state.settings.ollamaVisionModel`＝既定 `llava`。通常の文章抽出は従来の `ollamaModel`）、**Gemini** は `inline_data`、**Claude** は `image` コンテンツブロック。完全ローカルON（既定）では Ollama のビジョンモデルのみを使い**オフラインで画像を読める**（クラウドAIへは送信しない）。ビジョンモデル未取得（Ollamaが404）のときは「`ollama pull <model>`」を案内するエラーを出す。取得元ラベルは「貼り付け画像」。
  - **表形式は決定的にパース**: CSV（引用符対応の自前パーサ）・Excel（ローカル同梱の `browser/vendor/xlsx.full.min.js`＝SheetJS。CDN非依存でオフラインでも動く）・iCalendar（.ics の SUMMARY/DTSTART/DUE/ATTENDEE/ORGANIZER）。CSV/Excelは先頭10行から「タスク名/件名/予定/内容…」「期日/期限/締切/日付…」列を含むヘッダー行を探し、任意で「担当」「参加者/メンバー」列も読む。ヘッダーが見つからないCSVと .txt/.md 等の自由文は**選択中のAIプロバイダで抽出**（JSON応答。ローカルAI＝Ollamaは `format:"json"`+`num_ctx:8192`、Geminiは `responseMimeType:"application/json"`、Claudeは `messages.create` 非ストリーミング）→ **オフラインでもローカルAIで取り込み可能**。
  - 文字コードはUTF-8→失敗時Shift_JISで自動判別。Excelシリアル値・「8月28日」等の年なし和式日付（半年以上前なら来年扱い）も `yyyy-mm-dd` に正規化。AIが返す「未指定/不明」等のダミー担当者は空扱い。
  - URLは `fetch` 直接取得（HTMLはタグ除去して本文のみ）。CORSで取得できないサイトは、貼り付けかファイル保存経由を案内する。`.ics` のURLは決定的パース。
  - 読み取り結果はプレビュー表（チェックボックス＋全項目編集可、既存タスク・既出候補と名前+期日が同じものは自動除外）→「選択した予定をボードに反映（追加・変更）」で `status:"未完了"`・既定リマインド日数で一括登録。期日が読めなかった候補は期日未設定として登録される。
  - **参加者を名簿から選択**（2026-07-15追加、ブラウザ版のみ）: プレビュー表の各行の参加者セルに「👥 名簿から選択」ボタン（`.imp-pt-pick`）を置き、押すと `openImpParticipantPicker` が名簿（`state.contacts`）全員をチェックボックスで並べたピッカー（`#impPtPicker`）を開く。現在の参加者はあらかじめチェック済みで、チェックのON/OFFがその候補の `participants` に即反映し、行のテキスト欄（従来どおりカンマ/読点区切りの手入力も可）と同期する。名簿にない名前は下部の入力欄から追加できる（`assigneeList` datalist 付き）。ピッカーは `position:fixed` で**開いている `<dialog>`（`showModal` のトップレイヤー）内に追加**して重なり順を確保し、行の再描画・外側クリック・Esc・「完了」で閉じる。
  - **前回の類似イベントをデフォルトに参照**（2026-07-15追加、ブラウザ版のみ）: 新規候補（既存タスクに一致しない予定）で、取り込み元に担当者・参加者が無かった項目は、**前回の類似イベントの内容を初期値**にする。担当者は `findSimilarPastTask(name)`（`normalizeTaskName` で名前を正規化し、一致する過去タスクのうち id が最大＝最新のもの）の `assignee`、参加者は従来どおり `suggestParticipants(name)`（類似タスクの担当者＋参加者を出現回数・新しさで採点した上位5名）を採用する。取り込み元に値があればそれを優先し上書きしない。前回参照でデフォルトを補った候補には `similarRef`（参照した過去タスク名）を持たせ、プレビュー表の参加者セル下に「🔁 前回「◯◯」を参照」（`.imp-similar-hint`）を表示する。類似イベントが無い候補は従来どおり空のまま。
  - **反映先モード（新規作成／既存タスクに追加）**（2026-07-14追加、ブラウザ版のみ）: パネル上部の「取り込んだ予定の反映先」ラジオで、①**新しいタスクとして作成**（既定、従来どおり自動でボード振り分け）か、②**既存のタスクに予定を追加**（`#impTargetTask` セレクトで稼働中タスクを1つ選択）を選べる。②では反映時、選択した候補のうち**最も新しい期日**を既存タスクの新スケジュールとして採用し、参加者は**和集合で追加**、担当者は取り込み値があれば上書き。確認ダイアログ（`openConfirm`）で「◯◯: 期日 A→B／参加者を追加：…」を提示し、OKで既存タスクを更新（月が変わればボード移動）。新規タスクは作らない。`impMode` で保持し、`renderImpTargetOptions()` が `taskIsArchived` を除く稼働タスクを期日順に一覧化する。
  - **ファイル/フォルダ選択は純粋な `label[for]` 方式**（2026-07-14変更、ブラウザ版のみ）: 「📂 フォルダを選択」「📄 ファイルを選択」は `<button>`＋`input.click()` ではなく `<label for="…">`（`.imp-pick`、同幅で横並び）で、JSを一切介さずネイティブの選択ダイアログを開く（PC内のどこでも参照可能）。一度 File System Access API（`showOpenFilePicker`/`showDirectoryPicker`）を試したが、環境により失敗して「無反応」になるため**撤去済み**（2026-07-14）。`impFilesInput` に `accept` は付けない（付けるとダイアログで対象外ファイルがグレー表示になり「選べない」ように見えるため。形式チェックは `impHandleFiles` が行い対象外はメッセージ表示）。ドロップゾーン（`#impDropZone`、これも `label[for]`）へは**ファイルだけでなくフォルダのドラッグ&ドロップ**にも対応（`webkitGetAsEntry`＋`readDropEntry` で再帰）。
  - **予定表の更新＝変更点の検出と確認チェック**（2026-07-14追加、ブラウザ版のみ）: 更新版の予定表を読み込んだとき、`impAddCandidates` が候補ごとに既存タスク（`state.tasks`）を名前で照合する。**同名＋同期日**は「変更なし」として除外、**同名だが期日が異なる**候補は `changeType:"update"`（`targetTaskId`・`oldDue`/`oldAssignee`/`oldParticipants` を保持）として扱い、どの既存タスクにも一致しない候補は `changeType:"new"`。プレビュー表に「種別」列（🟢新規／🟡変更バッジ、変更行は期日セル下に「旧: yyyy-mm-dd」を併記）。反映時、変更候補が含まれていれば**汎用確認ダイアログ `#confirmDialog`（`openConfirm()` が OK/キャンセルを Promise で返す）**で「◯◯: 期日 8/1 → 9/1」等の差分（期日・担当者・参加者）を一覧提示し、OKで初めて既存タスクを更新（＝重複を作らず既存タスクを書き換え。**期日の月が変わった場合のみ該当月ボードへ移動**、`participants`/`assignee` は読み取れた場合のみ上書き）、新規候補は従来どおり追加する。キャンセル時は何も変更しない。
  - **所属ボードの自動振り分け**（2026-07-12、自由ボード統合後）: 期日の月と同名のボード（例「8月」）が存在すればそこへ、無ければ先頭ボードへ所属させる（新しいボードは自動作成しない。ボードが1つも無ければ「ボード1」を作成）。
- **参加者（participants）**（2026-07-12追加）: `task.participants`（文字列配列、旧データは未定義=空扱い）。カードの詳細展開に「👥 参加者」行があり、**担当者がチップUIで参加者を追加・削除**できる（名簿の datalist＋自由入力）。
  - **前回の内容からの自動提案**: `suggestParticipants(name)` がタスク名を正規化（数字・記号・「月日年度回」等を除去）して過去の類似名タスクを探し、その担当者・参加者を出現回数＋新しさで採点した上位5名を**点線チップの初期草案**として提示（クリックで追加）。取り込みプレビューでも参加者が読み取れなかった候補に同じ提案を初期値として入れる。
  - 名簿の「関与タスク」件数・担当タスク一覧は participants も含めてカウント。AIツール `add_task`/`update_task` にも `participants` パラメータを追加（updateは丸ごと置換）。システムプロンプトのタスクJSONにも含まれる。
- **通知メール送信リスト**（2026-07-12追加）: リマインダーチェックのモーダルに「📧 送信リストを作成」。期日ありの未完了タスクから **送信予定日（期日−リマインド日数）・内容（タスク名）・期限（残り日数付き）・送信予定者（担当者＋参加者、名簿からメール引き当て、無ければ「アドレス不明」）・状態（送信済/送信対象/期日超過/予定）** の一覧を送信予定日順に自動生成する。
  - 「⬇ CSVダウンロード」: BOM付きUTF-8 CSV（件名・本文列も含む。Excelでそのまま開ける）。
  - 行ごとの「✉ 作成」: `mailto:` で既定メールソフトの下書き（To=先頭宛先、Cc=残り、件名・本文入り）を開く（オフラインでもOutlook等が起動する）。
  - リマインド送信チェック（ドライラン）のログにも参加者のメールを Cc として表示。メール本文の宛名は受信者全員の連名。
- **終了ボード（ゴミ箱）**（2026-07-12追加）: ボードの**削除・完了はどちらも即消去せず「終了ボード」へ移動**する（Windowsのゴミ箱と同じ仕組み）。ボードの三本線メニューに「✅ ボードを完了（終了へ移動）」「🗑 ボードを削除（終了へ移動）」の2項目がある。
  - 実装: `board.archived = true` ＋ `archivedAt`（epoch ms）＋ `archivedReason`（`"完了"`/`"削除"`）。タスクの所属（`task.boards`）は変更しないため復元で完全に元に戻る。終了時に結合からは自動で外れる。最後の稼働ボードは終了できない。終了直後のトーストに「元に戻す」あり。
  - **終了中の扱い**: 稼働ボード一覧（`activeBoards()`）から外れ、ボード表示・所属ボードチップ・タスク追加先・取り込みの振り分け・AIのボード検索から除外される。**所属先がすべて終了済みのタスク（`taskIsArchived`）** は、ボード表示・リマインド判定・送信リスト・名簿の関与タスクから除外される（参加者提案の学習元としては引き続き使う）。AIのシステムプロンプトには「終了（アーカイブ済み）ボード」として名前だけ渡す。
  - **確認・復元**: 「🗑 終了ボード」でモーダル（`sectionArchive`）を開くと、ボード名・区分（✅完了/🗑削除）・終了日時・タスク数の一覧をいつでも確認できる。操作は3つ（2026-07-12更新）: **「↩ 復元」**（ボードとタスクがそのまま復活。位置が無ければ既定位置・最前面）／**「👁 中身の確認」**（行の下にそのボードのタスク一覧＝名前・期日・担当者・参加者・ステータスを展開表示。もう一度押すと閉じる。展開状態は非永続）／**「✕ 完全に削除」**（**confirm を2回**行ってから、ボードとそのボードだけに属するタスクを完全削除。共有タスクは所属から外れるだけ）。完全削除するまでデータは消えない。
- **背景の固定とフォルダ写真背景**（2026-07-12追加）:
  - **📌 固定ボタン**（アプリバー、`#btnBgPin`）: ONにすると `settings.bgFixed = true`。自動切替タイマーが停止し、表示中の背景（`settings.bgCurrent` に `{kind:"gradient"|"url"|"local", ...}` として常時記録）を**次回起動時にそのまま復元**する。固定中も「🖼 背景」ボタンや設定変更での手動切替は可能で、変更後の背景が新たに固定対象になる。ボタンは固定中「📌 固定中」＋ハイライト表示。
  - **背景の種類**（設定、旧「背景：ネット画像を使う」トグルを置き換え）: `settings.bgSource` = `"online"`（picsumランダム）/ `"local"`（フォルダの写真）/ `"gradient"`。**空のときの既定は常に `"online"`（画像背景）**。旧 `bgOnline=false` は引き継がない（グラデーションが単色に見えるため。2026-07-12変更。グラデーションにしたい場合は設定で明示的に選ぶ）。
  - ☰メニュー内で「🖼 背景」と「📌 固定」は横並びの1行（`.app-menu-row`）に配置する（2026-07-12変更）。
  - **フォルダの写真**: 「📂 写真フォルダを選択」（`webkitdirectory`）で jpg/png/webp/gif/bmp（15MB未満、最大200枚/回）を **IndexedDB**（DB `todoReminderBgPhotos`／store `photos`、Blobのまま保存）へ取り込む。取り込み後はオフラインでも表示できる。ランダム表示は直前と同じ写真をなるべく避ける。表示は objectURL（クロスフェード完了後に revoke）。設定に登録枚数表示と「登録写真をクリア」（confirm付き、元ファイルは消えない）あり。取り込むと自動で背景の種類が「フォルダの写真」に切り替わる。
- **ヘッダー構成とメニュー集約**（2026-07-12変更、2026-07-13再編）: アプリバーは「タイトル／検索ボックス／**▦ 新しいボードを作成**／**📥 取り込み**／**🤖 AI**／**☰ メニュー**」。よく使う3操作はヘッダー直置きで、残り（🖼背景・📌固定〔横並び1行〕／🗑終了ボード／🔔リマインダー／👥名簿／⚙設定）は「☰ メニュー」（`#btnAppMenu`→`#appMenu`）のドロップダウンから選ぶ。項目選択・外側クリック・Esc で閉じる（`aria-expanded` 同期）。メニュー内は元のボタン色を打ち消したフラット表示（📌固定中のハイライトのみ状態表示として残る）。
  - **ボードヘッダーのレイアウト**（2026-07-13変更）: 左＝ボード名ラベル（`flex:1`・省略記号）、**中央＝「現在のタスク数 N」ピル**（`.kb-col-count-center`、絶対配置。丸数字バッジから変更。タスク0件時は非表示）、右詰め＝達成率％・＋（タスク追加）・☰（メニュー）。
  - **ボードの拡大縮小の改善**（2026-07-13）: 右下コーナーハンドルを大型化（22px・ホバーで強調・Pointer Eventsでタッチ/ペン対応）し、**右端（幅のみ）・下端（高さのみ）のグリップ帯**からもドラッグ可能に。**コーナーのダブルクリックで既定サイズ（幅340px・高さ自動）にリセット**。下端を上までドラッグすると従来どおり折りたたみになる。
  - **ボード全体を約1.5倍サイズに拡大**（2026-07-14、ブラウザ版のみ）: カード本文の基準フォント 11px→16px（`--kb-font-scale` は従来どおり乗算）、カード幅 128px→190px（開いた選択カードは 300px→400px・17px）、ボード既定幅 240px→340px（`pos.w || 340`、ダブルクリックリセット・結合タイルの横並び間隔・ヘッダーgap計算も同値に更新）。ヘッダーは 13px→17px・タスク数/％バッジ 10px→13px・☰/＋ボタン 20px→28px・アバター 18px→26px。JS側の `clampPos`（cardW 190/cardH 100）、`ensureColPos` の初期グリッド（colW 380/rowH 420）、`COL_HEADER_H` 44 も追随。ユーザーが手動リサイズ済みのボード/カード（保存された `w`/`task.size`）はそのまま尊重される。
  - **タスク追加はボード内へ移設**（2026-07-13）: ヘッダーの「＋タスク追加」（`#btnAddTaskTop`）は廃止。タスク追加は①各ボードヘッダー右の**「＋」ボタン**（☰の左隣、そのボード宛のダイアログを開く）②ボード空きスペースのダブルクリック③キーボード `N`（先頭ボード宛）の3経路。
- **実装ルール：入力の trim と一意IDによる要素追跡**（2026-07-12整理）: すべてのユーザー入力は `trim()` してから検証し、**空白のみの登録を拒否**する（タスク追加ダイアログ・名簿の担当者追加・AIツール add_task/update_task）。名簿の名前はメール引き当てのキーのため**同名の重複登録も拒否**。一覧UIの行識別には**配列インデックスを使わず一意ID**を使う（取り込みプレビュー＝候補ごとの `uid`、送信リストの✉作成＝タスク `id`、カード＝`data-task-id`、ボード＝`data-board`）。行の増減や再構築でハンドラの参照先がズレるのを防ぐため。
- **ドラッグ&ドロップの演出は全廃**（2026-07-12）: カード/ボードのドラッグ中の発光・拡大・半透明、元位置の破線プレースホルダー表示、着地時のフェード（`glowFade`）、結合時のポップ（`popAnimate`）をすべて廃止した（表示崩れ対策とご要望による）。残っている視覚表現はドロップ先の**シンプルな枠線（outline）のみ**。`glowFade`/`popAnimate` は呼び出し互換のため空関数として残置。プレースホルダー要素（`.kb-card-ghost`）はドラッグ終了時にカードを元のDOM位置へ戻すアンカーとしてJSが使うため生成は続けるが `visibility: hidden`。

**デスクトップ版（VBA）は現状未対応・既知の差分**: `KanbanSheetCtrl.cls` はステータス3列（未完了/進行中/完了）の `MSForms.ListBox` 構成のままであり、月別パネル化・カードグリッド表示・クリックでの詳細展開/インライン編集のいずれも未実装。ListBoxの行は固定書式の複数列テキストのみで、行ごとに高さや表示内容を動的に変えることはネイティブでは行えないため、同等の挙動をVBA側で実現するには別コントロール（ラベル群やテキストボックス）を使った再設計が必要。着手する場合は本節を更新すること。
